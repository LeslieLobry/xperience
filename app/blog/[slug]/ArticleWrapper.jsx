"use client";
import ArticleViewTracker from "../../../components/ArticleViewTracker/ArticleViewTracker";

export default function ArticleWrapper({ articleId }) {
  return <ArticleViewTracker articleId={articleId} />;
}
